import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Jesus-grupoE191-examen2blank',
  webDir: 'www'
};

export default config;
